# Fedora system layer only — everything else comes from the Brewfile, which
# Homebrew installs on Linux just like on macOS.
#
#   one package per line, '#' starts a comment
#   '?' prefix = optional: if the repos don't have it, warn and move on
#
# Keep this list short. A package belongs here only if brew shouldn't own it:
# Homebrew's own build prerequisites, or something that must sit in a standard
# system path (login shell, nvim's python provider, JDKs, container runtime).

# ---- Homebrew build prerequisites ---------------------------------------
gcc
gcc-c++
make
procps-ng
curl
file
git
unzip

# ---- login shell (chsh needs a shell from a system path) ----------------
zsh

# ---- system python + neovim's python3 provider -------------------------
python3
python3-pip
python3-neovim

# ---- JDKs: 05-java and jdtls.lua resolve these from /usr/lib/jvm --------
java-17-openjdk-devel
java-21-openjdk-devel

# ---- fonts: fontconfig registers the Nerd Fonts 01-packages downloads ---
fontconfig

# ---- containers ---------------------------------------------------------
# Fedora retired moby-engine; podman is the drop-in replacement and provides
# a docker-compatible CLI via podman-docker. For genuine Docker CE instead,
# add Docker's own repo: https://docs.docker.com/engine/install/fedora/
?podman
?podman-docker
