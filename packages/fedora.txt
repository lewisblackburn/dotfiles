# Fedora bootstrap layer only — everything else comes from packages/tools.csv,
# which Homebrew installs on Linux just like on macOS.
#
#   one package per line, '#' starts a comment
#   '?' prefix = optional: if the repos don't have it, warn and move on
#
# Keep this list short. A package belongs here only if it has to exist *before*
# brew can work, or must sit in a standard system path. Ongoing tools that dnf
# should own (zsh, podman) live in the registry's `fedora` column as `dnf:` rows
# instead, so they show up in --coverage alongside their macOS counterparts.

# ---- Homebrew build prerequisites ---------------------------------------
gcc
gcc-c++
make
procps-ng
curl
file
git
unzip

# ---- system python + neovim's python3 provider -------------------------
# Deliberately NOT mise's python: nvim's provider has to be the interpreter the
# python3-neovim package installs into.
python3
python3-pip
python3-neovim

# ---- fonts: fontconfig registers the Nerd Fonts 70-fonts downloads ------
fontconfig
