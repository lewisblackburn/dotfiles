# Fedora package list — the Linux equivalent of the Brewfile.
#
#   one package per line, '#' starts a comment
#   '?' prefix = optional: if the repos don't have it, warn and move on
#
# Anything that isn't packaged for Fedora at all (gum, starship, deno, pnpm,
# kotlin tooling, nerd fonts) is handled by install/01-packages.sh, which falls
# back to each tool's official installer.

# ---- shell + core ---------------------------------------------------------
zsh
git
curl
wget
tmux
tree
unzip
ripgrep

# ---- build tools (treesitter parsers compile locally) ---------------------
gcc
gcc-c++
make
cmake

# ---- editor ---------------------------------------------------------------
neovim
python3
python3-pip
python3-neovim

# ---- languages -----------------------------------------------------------
nodejs
npm
golang
java-17-openjdk-devel
java-21-openjdk-devel
?maven
?gradle
?kotlin

# ---- cli tools ----------------------------------------------------------
?gh
?git-delta
?lazygit
?bottom
?dua-cli
?yadm
?tree-sitter-cli
?fzf
?jq

# ---- services / containers ---------------------------------------------
?redis
?moby-engine
?docker-compose
?docker-buildx
?kubernetes-client

# ---- media --------------------------------------------------------------
# ffmpeg proper needs RPM Fusion; ffmpeg-free is in the main Fedora repos.
?ffmpeg-free

# ---- fonts --------------------------------------------------------------
# Nerd Font patched variants aren't in the Fedora repos — 01-packages.sh
# downloads them from the nerd-fonts release into ~/.local/share/fonts.
?fira-code-fonts
